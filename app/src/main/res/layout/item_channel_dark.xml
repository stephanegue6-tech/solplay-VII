<?xml version="1.0" encoding="utf-8"?>
<!-- Identique à item_channel.xml mais avec des couleurs de texte adaptées à un fond
     sombre/transparent : utilisé par le panneau "Changer de chaîne" de l'écran de lecture,
     qui reste en surimpression de la vidéo. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="12dp"
    android:focusable="true"
    android:clickable="true"
    android:background="@drawable/bg_list_item_selector">

    <ImageView
        android:id="@+id/ivChannelLogo"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:scaleType="centerInside"
        android:background="@color/solplay_search_bg_overlay" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvChannelName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/solplay_white"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvChannelGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/solplay_white_60"
            android:textSize="11sp" />
    </LinearLayout>

</LinearLayout>
